apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "stack-backend.fullname" . }}
  labels:
    {{- include "stack-backend.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
